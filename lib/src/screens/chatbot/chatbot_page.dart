import 'package:flutter/material.dart';
class MemberChatBotPage extends StatelessWidget {
  const MemberChatBotPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Chat Bot Page")));
}