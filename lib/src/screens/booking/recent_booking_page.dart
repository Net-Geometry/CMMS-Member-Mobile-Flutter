import 'package:flutter/material.dart';
class RecentBookingPage extends StatelessWidget {
  const RecentBookingPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Recent Booking Page")));
}