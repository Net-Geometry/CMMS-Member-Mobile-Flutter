import 'package:flutter/material.dart';
class UpcomingBookingPage extends StatelessWidget {
  const UpcomingBookingPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Upcoming Booking Page")));
}