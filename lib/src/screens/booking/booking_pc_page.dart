import 'package:flutter/material.dart';
class BookingPCPage extends StatelessWidget {
  const BookingPCPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Booking PC Page")));
}