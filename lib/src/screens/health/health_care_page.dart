import 'package:flutter/material.dart';
class HealthCarePage extends StatelessWidget {
  const HealthCarePage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Health Care Page")));
}