import 'package:flutter/material.dart';
class MemberPersonalInfoPage extends StatelessWidget {
  const MemberPersonalInfoPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Personal Information Page")));
}