import 'package:flutter/material.dart';
class MemberFAQPage extends StatelessWidget {
  const MemberFAQPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("FAQ Page")));
}