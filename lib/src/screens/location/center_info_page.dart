import 'package:flutter/material.dart';
class CenterInformationPage extends StatelessWidget {
  const CenterInformationPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Center Information Page")));
}